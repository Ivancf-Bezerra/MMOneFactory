/** Bundle publicado (ex.: pasta `docs/` no GitHub Pages). */
export const environment = {
  production: true,
  /** Acesso de teste à UI sem API (mesmo fluxo que em localhost). */
  localDemoAuth: true,
  showLocalApiCredentialsHint: false,
  negotiationInboxUseMock: true,
};

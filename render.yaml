# Blueprint Render: site estático em docs/ (build Angular gerado fora deste repo).
services:
  - type: web
    name: mmsite
    runtime: static
    staticPublishPath: ./docs
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
